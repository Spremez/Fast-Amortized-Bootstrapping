# Stage326 Reproduction Commands

```bash
python3 scripts/build_stage326_exact_dense_route_closeout.py
```

Stage326 is an evidence closeout. It does not run new benchmarks or modify
scalar SAB/default `sab_pvw_*`.
