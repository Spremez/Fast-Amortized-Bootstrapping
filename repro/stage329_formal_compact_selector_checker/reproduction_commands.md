# Stage329 Reproduction Commands

```bash
python3 scripts/build_stage329_formal_compact_selector_checker.py
```

This stage runs a finite checker only. It does not change `sab_pvw_*`, scalar
SAB, or key generation.
