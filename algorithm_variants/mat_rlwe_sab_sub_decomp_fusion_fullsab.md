# MAT-RLWE SAB Sub-Decomp Fusion Full-SAB Variant

Flag:

```text
SAB_PVW_SUB_DECOMP_FUSION=true
```

Default status:

```text
off
```

The scalar SAB path and default PVW path remain unchanged. This variant only
changes the explicit `sab_pvw_*` CMUX hot path.
