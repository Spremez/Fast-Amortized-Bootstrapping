# GOAL: CCF-A 类安全会议论文 —— 自主执行状态

角色: 本 Agent 作为论文唯一作者，负责算法设计、实验、撰写全流程。
目标 venue: CCS / USENIX Security（CCF-A；USENIX Sec'27 周期为主目标，CCS'27 为备选）。
更新: 2026-08-20（自主模式启动）

## 服务器 E1 矩阵收官（2026-08-25 05:08Z，11/11 failures=0）

新服务器（luck-Precision-7920，双路 Xeon Gold 6230R，104 线程/251GB）全矩阵：

| 行 | 均值 | 范围 | 噪声 |
|---|---:|---|---|
| SET_4_5_2048 r4 | **1.8271×** | 1.762–2.008 | Pass |
| SET_4_5_2048 r2 | 1.8248× | 1.800–1.980 | Pass |
| SET_2_3_2048 r4 | 1.8217× | 1.781–1.998 | Pass |
| SET_2_3_2048 r2 | 1.8002× | 1.690–1.861 | Pass |
| SET_6_7_4096 r4（Boot6 对标） | 1.7808× | 1.749–1.802 | Pass |
| SET_4_5_4096 r2 | 1.7508× | | Pass |
| SET_2_3_4096 r2 | 1.7384× | | Pass |
| SET_4_5_4096 r4 | 1.7030× | | Pass |
| SET_2_3_4096 r4 | 1.6877× | | Pass |
| SET_8_9_4096 r4（Boot8 对标） | 1.6350× | | Pass |
| SET_8_9_4096 r2 | 1.5152× | | Pass |

服务器范围 **1.5152×–1.8271×**（本地 WSL 为 1.5693×–1.7476×，跨平台互证）；
2048 行在服务器上最高（单核 2.1GHz 有利），8-bit 行最低（与 BatchBoot 自报
6-bit 饱和 1.05× 的趋势同向）。汇总已回收：
`repro/stage355_server_e1_matrix/stage355_summary.csv`。

## 里程碑（关键路径）

| # | 里程碑 | 状态 | 依赖 |
|---|---|---|---|
| M1 | D1 新颖性门 | ✅ PASS（0d444d6） | 9 源哈希绑定 |
| M2 | D2 算子闭合门 | ✅ PASS + 权威重放（b3cf1e9），γ=2 | — |
| M3 | **D3 噪声/安全/成本门** | ✅ **PASS + 权威重放（账本 2c6236c，controller efc47d9）**；悲观投影 1.4599× ≥ 1.10 | stage322/331 实测数据 |
| M3+ | **D0-D3 全准入** | ✅ **`ADMIT_CANDIDATE_D_TO_ISOLATED_ENCRYPTED_OPERATOR_IMPLEMENTATION`，production_hot_path_permission=yes** | M1-M3 |
| M4 | D4-D6 隔离实现→集成→优化 | 🔨 下一主任务（D4：SAB_Operator_State/Key 独立类型，r=1/2/4 相位等价 + microbench，服务器跑） | M3+ |
| M5 | 全矩阵实验（stage355, 11 行） | 🔄 服务器运行中 | — |
| M6 | B2 BatchBoot 复现（head-to-head） | ⏳ | 服务器空闲窗口 |
| M7 | 论文全文 | ⏳ 骨架先行 | D3 结论已定：主贡献 = late-binding 算子 SAB + 强制重随机化发现 + 闭合定理 |
| M8 | 工件打包 + 投稿 | ⏳ | M4-M7 |

### D3 关键科学结论（论文素材）

1. **强制重随机化定理（新发现）**：late binding 把噪声放大 β=√2·‖ΔF‖₂（最坏任意
   LUT、N=2048、2-bit 时 β=32），高斯尾指数除以 β² → 直接绑定把标量 2⁻¹²⁰ 退化到
   2⁻⁰·¹²。构造因此**必须**携带 post-binding 重随机化 KS（σ_flood=2⁻⁸），
   恢复 2⁻¹⁸⁴ ≤ 2⁻¹²⁰ ≤ 2⁻⁶⁴。这是设计文档预留 `d4_rerandomization_if_required`
   钩子的实证激活——论文的核心理论贡献之一。
2. **Amdahl**：选择子环乘 25H→8lH（r 无关），中央 1.86×/悲观 1.46× over B1（>10% 门）。
3. **资源**：D key ≈ B0a 标量钥匙 + 重随机化 KS ≈ 18.7MB ≤ 2×B1（19.2MB）；内存 2g=4
   组件 vs B1 的 (1+r)=5 体。
4. 治理修复：D3 契约遗漏 finite_linear 注册（传递导入必选）→ 新 controller commit
   efc47d9（沿链前进，符合契约的 controller-pinning 模型）。

## 资源分工策略

- **本地（Windows/WSL, 16 核 12GB）**: 理论（检查器/证明文档）、D1-D3 治理流程、
  LaTeX 撰写、小规模验证。WSL 仅做构建冒烟。
- **服务器（autovoice-delld, 104 核 251GB, 6230R）**: 全部正式基准（stage355+）、
  D4-D6 kernel 基准、B2 复现。规范目录 `/home/spz/Fast-Amortized-Bootstrapping-stage355-e1-server/`。
- **并行原则**: 服务器跑数期间本地推进理论/写作，互不阻塞。

## 决策规则（自主执行时遵循）

1. **治理优先**: 仓库的 admission 流程（D0-D8）是论文可信度的骨架，不绕过；
   production hot-path 权限在 D3 PASS 前保持 false。
2. **诚实口径**: 论文声明必须落在已验证证据内（claim matrix 红线沿用）；
   投影数字明确标注为投影。
3. **止损**: D3 REJECT → F1（MAT-EMPmul 系统论文）为主轴；D3+M5 均不利 →
   TCHES 降级路线（roadmap §5 go/no-go）。
4. **服务器礼仪**: 单次任务化、断点续跑、不占满 104 核（构建 -j32）、磁盘 97% 满不加压。

## 当前会话执行队列

1. ✅ D3 实现 + 全准入（见上表 M3+）
2. ✅ 批判性接收评审（docs/d4_implementation_plan.md §0）：判定"理论已足、
   实测为零"，接收策略定为 Pareto（时间≈BatchBoot 级 + 密钥 3.3–11.5× 小
   + r-LUT 语义 + 机器验证证明），硬判据 A-E 落档
3. ✅ D4 实现契约：include/sab_operator.h（数据结构 + 7 个更新律 API）
4. 🔨 D4 C 实现（src/sab_operator.c + 等价测试 + microbench）——下一主任务
5. stage355 服务器监控（行1 第5样本）
6. B2 复现 + 应用原型（D4 等价测试通过后上服务器并行）

## 运行状态速查（2026-08-23）

- 本机: D4 参考实现已提交主树（`53e7ca9`，编译零警告）；下一项 = 等价测试模式
- 服务器: **2026-08-23 全天不可达**（ping 100% 丢包，疑似关机/离网；需操作者开机）。
  stage355 断点续跑就绪，恢复后自动从缺行继续；数据在盘上安全（最后一次确认：
  行1 SET_8_9_4096_r4 完成 4/10 样本，05:44Z）
- 已修复: 主树 `.git/config` 残留 `core.worktree`（备份于服务器前 /tmp 与本会话记录）


### v3 最终设计（原语探针后确定，2026-08-24）

独立探针（src/probe_mul.c，已提交 924271f）：`polynomial_mul_torus` 对
"满刻度 × 小数值"返回零（N=1024 复现）或 −(A·B)·2⁻⁵³ 的 ε 残差（N=256）；
`init_fft` 已排除。全库通行的正确模式是 trgsw.c:447 外积：
**`trlwe_decompose` 分解密文侧（小数位）× DFT 域满刻度多项式
（`trlwe_DFT_mul_addto_by_polynomial`）**。
v3 绑定 = U 通道按 accumulator gadget（bg_bit=23，l≈3 层）分解，
逐层与 DFT 域 F/τ(F) 相乘后精确移位累加——与系统全部外积同构，
成本 ≈ 3 EP（D3 late_binding_transforms 预算内）。


### v5/v6 与语义证据链（2026-08-24 深夜，探针 src/probe_v6.c）

- v5（精确截断拆分）与 v6（数位×key-DFT，镜像 trgsw.c:447）均数学正确但输出全零；
- setup 经逐项打印验证完全正确（s/pos/符号/±2^62）；
- 量级扫描（16 组）显示 to_DFT→mul_DFT→inverse 对稀疏尖峰输入给出 **纯整数卷积
  mod 2^64** 语义（7/8 行精确符合 (A*B) mod 2^64，非 torus 实数积 (A*B)>>64）——
  这解释 v2-v6 全部症状：2^62 通道系数与 F 的整数积 mod 2^64 仅保留 F 的低 2 位；
- **剩余唯一工作**：精读 execute_reverse_torus64 / polynomial_mul_DFT 的 split-hi/lo
  重组契约（双块布局：Re∈[0,N/2)、Im∈[N/2,N)），按其真实语义重写绑定缩放算术。
  这是收敛后的单点任务，证据链已完整。

### v7 突破（2026-08-25，提交见日志）

- **bind 构造验证正确**：整数谱语义下的 v7 = F 谱预缩 2^-62（表示 4·F_real）× 通道谱
  （2^62 类）单次积 + mod-2^64 归约——独立探针 res=65535/196608（1-ulp）；UNIT 修复
  （探针清零 + 通道累加缺失）后主程序 UNIT 亦通过；
- **stage1（setup+bind）仅剩 2^-20 级 FFT 噪声**（>>44 粗比较 ±1 单位），远低于 2^-4
  消息网格——结构正确；
- **剩余 bug 收窄到调度循环**（完整调度后消息级 75% 失配）：已推导确认 C 的
  NCMUX 无负号正确（τ 自同构作用于密文消息即给出 swap+τ 律，与标量 C 一致）；
  嫌疑收敛为蝴蝶/乒乓/下标的机械错误——下一步用逐步比较定位；
- 附带发现：WSL /tmp 在调用间被清（此前多次"空输出"的元凶），改用 /mnt/d 持久路径。

### 逐步比较诊断（2026-08-25 深夜）

- **bind 两分支单元级全部验证通过**：UNIT（id 通道）65535/196607；UNIT2（τ 通道）
  65535/-196609——此前 UNIT2 的"期望符号"是我漏算旋转负环，实测正确；
- **STEP 0（第一次蝴蝶+sub_a）即坏**（75%）；MID（仅蝴蝶）亦坏——bug 在蝴蝶内，
  sub_a 无辜；
- **通道转储三重特征**：① id 通道 b 分量系数精确 2 倍（8.38M≈2×4.19M=2×2⁶²/2²⁰）；
  ② τ 通道异常全满（D2 无负号律下应仅 ~12% 环绕 slot 有内容）；③ 峰值位置差 2；
- **下一焦点**：NCMUX 的 trlwe_eval_automorphism(2N−1) 对通道密文的实际效果
  （候选：KS key 的方向/或 CMUX 的 tmp 交互把内容复制/叠加）——特征组合
  "两倍+τ 满"最像"automorphism 输出被叠加而非替换"或"KS 方向错导致双份"。

### 微测试（2026-08-25 夜末，下一会话起点）

单调用微测试（main.c MICRO 块）：手造平凡通道对（id=¼X^10/X^30，τ=¼X^20/X^40），
sel[0]=TRGSW(1)。结果：**NCMUX out0/out1 与 CMUX out0 的输出峰全部落在位置 0/1
且符号为负**（期望 N−40 / N−30 / 30 的反射/直通）——与输入位置无关的确定性错误。
单次调用、微型、全插桩——下一会话从此处继续：嫌疑 = RGSW_encrypt_bits 生成选择子
的正确性（与生产调用的参数差异）、或 aut_minus1 KS key 的 gen/N 匹配。

### v11 突破（2026-08-26 凌晨，本会话）

- **v11 = 数位分解(≤2²² 小侧) × 预缩加权谱 spec(F·2^(23d−62))**——全部层乘积 ≤2^80
  （mask 2⁸⁵ 死区之下），重建 Σ dig_d·2^23d·F·2^−62 = F·comp/2^62 精确；
- **验证结果**：UNIT（平凡 id）**完美恢复**（65535/196607）；UNIT3（真密文）
  **id 路径完全正确**（[31]=65535✓）；拆分测试的 mask 垃圾（±8×）**全部消失**
  （SPLIT 残余仅 ±1 噪声级）；最终 gate 失配从 157 万降至 **2048**（纯 F 对位置）；
- **剩余有界问题（τ 谱路径）**：[39] 幅值对符号反（期望 −65536 得 +65536）、
  [35]=−2（期望 0）、4 个固定位置（[25][29][41][45]）残留 ¼/½ 尺度垃圾；
  翻转 τF 符号试验使 [35] 归零但损坏 [31] → 非简单符号问题，τ 谱存在固定腐蚀源
  （候选：τF 的 to_DFT 前未清零 wrap 缓冲/谱索引 dft[7..9] 边界）；
- 本会话完整提交链：`33cce8f`→v11（本提交）。

### UNIT4 判定性突破（2026-08-26，提交 c897d57 后）

- **UNIT4（双通道同时平凡）三项全部精确正确**：[31]=65535(+1✓)、[35]=393215(+6✓)、
  [39]=65535(+1✓)——此前的"τ 符号翻转"是**期望计算错误**：负环卷积
  X^2043·X^40 = X^2083 = −X^35（越过 2048 取负），修正后的真期望是
  **[31]=+1, [35]=+6, [39]=+1**——与实测三项全对；
- **谱槽位验证正确**（SPEC 调试：F 槽=F 精确，τ 槽=τF 精确含负号）；
- **bind 的代数/谱/重建层全部确认正确**；
- **剩余唯一问题（有界）**：真密文（mask≠0）时 4 个固定交叉位置
  （[25][29][41][45]）残留 ±¼/±½ 尺度、随运行变化的垃圾（平凡情形为零）——
  纯 mask 相关，候选为数位层 d=2 的 2^88 类乘积精度（对照：EP 的 2^107 类
  乘积在生产中正常，故也可能是别的结构因素）。

### 噪声触发泄漏定位战（2026-08-26 深夜）

- **对照实验链**：同值平凡密文（携带 o3 精确相位值含噪声）产出与真密文**相同的垃圾**
  → 垃圾跟随**通道值上的噪声**而非密文结构；o3 相位全位置转储干净；
- **(bg,L) 扫描**（bind 上直接扫，env 参数化）：bg=32/L=2 为平凡最优（d=1 层乘积
  2^73），但噪声情形所有组合仍出垃圾；
- **层贡献插桩（LAYERP）**：噪声低数位层（d=0）贡献 F 尺度内容（−3×@31）——
  但跳过 d=0 层垃圾仍在 → 泄漏非单一层；
- **量级特征**：垃圾恒为 ¼/½ 尺度（=通道内容尺度）= F⊛内容×4 的未缩放泄漏量级，
  随噪声实现逐运行变化；
- **谱域累加探针**（probe_layer.c ACCUM 测试）结果与 bind 行为不一致（bg23/3 探针
  ACCUM=0 但 bind 失败）——探针未复现 bind 的完整 4-积累加路径；
- 下一入口：扩展探针至完整 (g,d)×2 通道 4 积累加 + 噪声输入，或对 dft[1] 累加
  缓冲做双精度求和顺序审计。

### v4 进展（2026-08-24 晚）

v3（数位分解 + torus 域逐层移位）失败：中间层实数值 >1 不可表示（数位×F 的
torus 表示 wrap）——EP 的真实做法是 DFT 域累加、单次逆变换。v4 据此重写：
F 的带符号位层（每层 ≤½ 合法 torus）× U 的 DFT（预计算一次），DFT 域点乘累加，
单次逆变换，尾端 <<2 吸收 ¼ 通道缩放。**最终失配 94%（v3）→ 0.1%（2113/2.1M）**。
剩余：stage1 稳定 2048（半数 slot 特征）+ 单元探针零——一个有界 bug，疑似
位层权重/边界符号层的具体定义（层权 2^d 与移位提取位不匹配的候选已记录在
src/sab_operator.c v4 注释与本次会话）。下一步：修正层权定义为
bit(64-prec+d)·2^{64-prec+d} 的 torus 缩放并处理 d=prec 边界，重跑等价测试至 Pass。

## D4 进度

- [x] include/sab_operator.h（契约）+ src/sab_operator.c（参考实现，零警告编译）
- [x] main.c `SAB_OPERATOR_EQUIV_TEST` 运行（构建/执行/插桩全通）——**测试装置本身验证有效**
- [ ] 通道编码 v2：等价测试暴露两个 GF(257) 检查器结构性看不到的真实编码问题：
  1. **单项式不可表示**：torus 上裸整数 1 = 2⁻⁶⁴，算子通道不能携带 X^s 原尺度过
     Δ_op=½ 缩放 + 绑定乘 2F（2F∈(−1,1) 恒可表示，乘积精确）——已实现；
  2. **FFT 精度**：polynomial_mul_torus 为双精度 FFT 基，满刻度 2⁶³ 通道系数使
     绑定舍入误差 ~2⁻²⁰ torus（实测 50% 粗粒度不匹配，与理论吻合）。需要 gadget
     式分解绑定（F 数位分解 + 精确 xai 移位 + 小系数 FFT）或指数域绑定——v2 设计
     待做，属 D3 binding_domain"Torus-scale 证明"义务的实现侧。
  D2 检查器无法发现这两点（GF(257) 精确算术 + 共模调度），D4 等价测试的价值实证。
  3. **+½≡−½ 与原语逐项 wrap**（v2 已实施 + v3 方案确定）：½ 缩放无法编码负环符号
     （2⁶³≡−2⁶³）；¼ 缩放 + F 数位分解后，单元探针（U_id=¼X⁰）证明 bind 仍全零——
     `polynomial_mul_torus` 对 2⁶²×数位 的乘积逐项 mod-2⁶⁴ wrap（2⁶⁶≡0）。
     **v3**：U 系数拆 32 位高/低半（各 ≤2³⁸，FFT 安全），int128 精确重组
     （(chunk·U_hi)·2³² + chunk·U_lo，末次 mod 2⁶⁴ 即 torus 语义），再统一。**v3 最终版另见下一条（原语规范用法）。**
     2^(shift+2) 移位。阶段数据：stage1 失配从 1,045,011（满刻度 FFT）→ 2048
     （wrap 归零定位）→ 待 v3。
- [ ] microbench + D5 集成（服务器恢复后；服务器 8-23 起不可达待开机）

### 4-积路径独立探针（2026-08-27 会话，主执行窗口合并推进）

按交接胶囊计划构建 `src/probe_bind4.c`（无密码学、完整复刻 bind 内层 4-积路径，含 bg/L 与噪声参数化、__int128 精确参照、逐层/交换/符号消融）。**四项可复现硬结论**（ffnt 与 spqlios 双后端逐位一致、确定性种子）：

1. **复现成功**: 完整 4-积路径 + 通道噪声（2^44/2^49/2^54）出现内容尺度泄漏，量级 = 噪声高字 ⋆ F·2^{bg·d-62}·√N（三级噪声全部吻合）；无噪声 = 精确（UNIT4 复现）。
2. **谱域累加无罪**: 逐层独立逆变换 + 整数求和 ≡ 谱域累加单次逆变换（差 2^14 = FFT 地板）——胶囊"累加嫌疑"被推翻，泄漏在含噪声数位层的**单次外积信封**。
3. **触发器 = 乘子负号 + 通道噪声**（缺一不可）: 同一通道噪声 × F（正号）乘子 = 精确（2^14）；× tau_F（负号镜像）= 泄漏 2^53；乘子对调实验证明泄漏**跟随乘子而非通道**；正号镜像乘子（pos_tau）= 精确——位置镜像无关，**负号是必要条件**。
4. **信封语义统一**（消解全部表面矛盾）: `execute_reverse/direct_torus64` 信封 = 真卷积 **mod 2^64（低字）**，通过 x/2^64 的 frac 以双精度解析低位，前提真卷积 ≲ 2^116——SQ 内核（卷积 ≤2^63 无回绕）、D4 UNIT4（尖峰）与"小操作数整数卷积"探针在此语义下全部自洽；"实数 torus 积"是溢出区的表象。

**仲裁进行中**: 最坏失配点 k=300 的 Python 精确仲裁显示信封输出 ≠ 整数精确卷积（信封 −1.37e15 vs 精确 +9.41e15），但转储记账存在瑕疵（mult 转储值与源常数不符，疑层标签错位），仲裁未最终闭合——下一会话从修复 dump 记账继续（预期一次运行定案）。

**其他**: `sab_operator.c` 调试块 line 283 的 `62 - 23*d` 硬编码残留已修（仅影响 SPECTRUM_DEBUG 打印，非泄漏源）。

**下一步**: (a) 修 probe dump 记账 → 闭合仲裁；(b) 若信封在"负乘子 × 稠密无符号数位 × 回绕项"下确错，定位 execute_direct_torus64 标量解码或 polynomial.c 共享胶水；(c) 闭合后跑 SAB_OPERATOR_EQUIV gate。

### 仲裁续（2026-08-27 P0 窗口第二波）

新增 `src/probe_envneg.c`（符号×位置×构造要素全扫描的最小复现）：**±尖峰乘子×稠密u32数位×全部回绕位置 = 全部精确**；bind4 g=1/d=1 层的逐要素忠实复刻（尖峰+噪声 dig、双负尖峰 2^60 级谱预缩放乘子，mode 7）也**精确**（噪声至 2^60 档仍精确）。新事实：bind4 内 d=1 数位含 2^32 级值 = 负噪声字的算术右移符号扩展（补码数位表示的合法成分，独立复刻含同类值仍精确）。

**当前判定**: 泄漏的"层内代数"已被独立复刻排除；剩余矛盾集中在 bind4 探针自身的检查/转储路径（此前转储 mult 值与源常数不符的同源疑点）。真实 bind 的垃圾是在真实密文输出上观察到的（非检查路径），故真实泄漏仍真实。下一会话：在 bind4 内联一份位相同计算的独立复刻做对照，或直接弃用 bind4 检查、回到真实 bind 上用 (bg,L)/符号消融复扫。服务器 P0-a（SQ v3 AVX-512）已启动（spz:~/spz/fab-main, PID 3581069）。
