# SQ 在矩阵自举工作中的地位与贡献：完整叙事

Date: 2026-09-04
回答用户问题（2026-09-04）：矩阵外积中对 c 的 k+r 个分量做数字分解 G⁻¹(c)
与 SQ 是不是一个算法、关系是什么、SQ 对整个矩阵自举的地位与贡献。
本文档为论文叙事的正式底稿（§3 与 §5 的骨架素材），全部主张已绑定既有
定理/实测，无新增未证内容。

---

## 1. 界面抽象：外层算法对核算术层只有一个契约

矩阵自举的外层（调度、结构、尾声）对"外积怎么算"只有一个接口契约：

```text
原语 ⊡：给定 Mul-GSW 选择子 C（加密对角消息 m'）与 Mul-LWE 操作数 c，
       输出 C ⊡ c 满足
         φ_q(C ⊡ c) = m'_q · φ_q(c) + ε_q     （M1，逐 lane 相位作用）
         ‖ε_q‖ 有多项式界，常数由核决定        （M2，无跨体放大）
```

凡是实现该契约的核都可插入。**外层算法本身不包含 G⁻¹，也不包含量化——
这两者都属于 L3 核算术层的两种可选实现。**

## 2. G⁻¹(c) 与 SQ 的关系：同一槽位的两种机制，非并列算法

**G⁻¹ 为什么存在**：标准外积要把"加密的乘子 m"作用到 c 上，同时把选择子
噪声的放大系数压在多项式界内。做法 = 把 c 的每个分量**数字分解**（提取
≤Bg 的小数位），用小数位去乘 gadget 缩放的选择子行。分解不是优化，而是
标准核中**噪声有界的使能机制**；矩阵化只是把分解对象从 k+1 个分量扩展为
k+r 个分量（1+r 行），机制本身未变。

**SQ 为什么可以不做分解**：SQ 更换的是**操作数的表示不变量**——累加器
全程维护在 Q=2^q 尺度（系数为 |·|≤2^{q-1} 的小整数）。操作数本身已经
"小"，噪声有界的职能由**尺度量化本身**接管：乘子对选择子噪声的放大系数
从"数位 ≤ Bg"变为"操作数 rms ≤ 2^{q-1}"，噪声系数 σ·2^{q+3.5}（q=16）
vs σ·2^{27.1}（Bg=2^23）。计算结构变为：整数卷积（mod 2^64，无回绕不变量
保证）→ 一次融合重缩放（>> 64−q）。消息路径**逐系数精确**（定理 1：
消息项低 64−q 位为零 ⇒ 所有 R_q 恒等，舍入只进误差项）。

**两者的精确数学关系**：l=1、Bg=2^23 的标准核就是 SQ 家族在 q=23 处的
成员——数字提取 ≈ 隐式 q=23 量化（每次外积对满 torus 破坏性执行），
gadget 尺度 h_0=2^{41} = SQ 的消息尺度 2^{64−q}；两噪声公式在 q=23 处
一致（sq_theory_rigorous 定理 2 已核）。SQ 的三步推进：

1. **显式化**：量化从"每次外积的隐式数字提取"变为"全程维护的尺度不变量"；
2. **参数化**：q 与 Bg 解耦成为自由设计旋钮（受 p 域与 σ 需求双约束）；
3. **可证明**：消息路径精确性（定理 1）+ σ_G 系数推导（定理 2）+ p 域
   闭环（定理 3，p≤5@q16 实测边界）。

一句话：**G⁻¹ 是标准核的噪声有界机制；SQ 用尺度不变量接管同一职能，
从而消除分解这一步。二者是同一契约下的两种核，不是两个层。**

## 3. SQ 对整个矩阵自举工作的地位与贡献（四条，按重要性排序）

### 3.1 安全硬化使能者（不可替代的贡献）

CRYPTO'26 稀疏密钥混合攻击修正下，系统需在 ≥128 bit 约束下恢复参数。
SQ 的 σ 余量 2(23−q) 是唯一实测可行的吸收通道：

- 否定性结果（命题 2.7，实测）：σ 单旋钮对 SQ 与标准核**同时失效**
  （KS/自同构噪声 ∝ σ·2^{Bg_ks} 线性放大，每槽 ~140 次 aut-KS 累计）；
- 正面闭环（σ+KS 联动，实测）：σ_out+15 时 SQ 通过（门 0 失配）而标准
  核失效；FINAL 定案（h=42/σ_G=2⁻⁴⁹，stage371 全量）下 SQ 标量
  **686/SQ = 1.117×**，噪声仅 +0.08 bit；
- 对照：若无 SQ，标准核只能靠 h 上调（h=52 平衡层实测 r-lane +17% /
  标量 +32–45%，且 σ 收益归零——stage363）。

**定位：SQ 使"修正安全下的快速矩阵自举"成为可能——这是整个系统在
≥128 bit 口径下仍然快于 686 的原因。**

### 3.2 速度（次要、诚实的主张）

- 标量：内核消除分解通道，−4.5%~−7.3%（9 轮中位/min），全自举 1.117×；
- 矩阵 r-lane：分解已被矩阵批处理共享（1+r 行），SQ 边际收益归零
  （实测平价 1.004–1.016）——如实声明，不作为矩阵侧速度主张。

### 3.3 理论贡献（"prove" 的一半）

1711（本团队前作）给出尺度化外积思想；本工作在稀疏摊销 SAB 语境下
实例化并补全可证性：定理 1（消息路径精确性——NCMUX 蝶形调度下逐原语
归纳，此为 1711 未覆盖的调度形态）、定理 2（σ_G 系数闭式，实测标定
2^{q+3.5}）、定理 3（无回绕不变量与 p 域）。口径：instantiate/adapt/prove
+ 作者重叠披露。

### 3.4 适用域边界（诚实边界，本身是结果）

SQ@q16 的舍入地板 2^{−56.6} 使 p≥7 失效（stage369 实测 46/2048 失配 +
定理 3 闭环）；标准核仍是 p≥7 的回退。SQ 与标准核是**互补域**而非替代：
外层算法按消息精度选核（设计文档 D4/C6 的混合核调度即此边界的利用）。

## 4. 叙事总结（论文口径的一段话）

> 完整矩阵自举算法（外层：稀疏域调度、多体结构适配、尾声与参数/安全
> 定案）定义于一个抽象外积契约之上；契约有两种可证实现——标准 gadget
> 分解核与尺度量化核 SQ（源自本团队 1711 前作，本工作证明其消息路径
> 精确性与噪声系数）。SQ 的价值不在矩阵侧速度（分解已被批处理共享，
> 实测平价），而在于其 σ 余量使 CRYPTO'26 修正下的参数恢复几乎免费
> （+0.08 bit 噪声换 2.4 bit 安全余量），从而让修正安全后的系统保持
> 对 686 的 1.12× 标量 / 1.21× r-lane 优势；其 p≤5@q16 的适用域边界
> 由定理与实测共同给出，与标准核形成互补域。

## 5. "SQ 能否完全替代 ⊡"：不可以，且在数学上不成立（2026-09-04 补）

**关键事实：标准核就是 SQ 家族的 q=23 成员。** l=1 时，标准核
（Bg=2^23，h_0=2^{41}，每次外积对满 torus 做破坏性数字提取）与
SQ(q=23)（全程维护 2^23 尺度不变量）计算同一函数、噪声公式在 q=23
处一致（定理 2 已核）。因此：

- "SQ 完全替代标准核"若取 q=23，则替代物与被替代物相同（无意义）；
- 若取 q<23（如 16）以获取硬化余量，则**必然付出精度域代价**——
  数位预算 (q) 同时约束三个量，汇率固定：

```text
q 每降 1 bit：  σ 吸收容量 +2 bit（σ 系数 σ·2^{q+3.5}）
              舍入地板改善 +1 bit
              无回绕/内容约束 q ≥ p+10 ⇒ p_max −1（实测 p≤5@q16）
```

**取舍表（全部为实测/定理背书）**：

| 轴 | SQ(q=16) | 标准 ⊡(Bg=2^23) | 谁优 |
|---|---|---|---|
| 精度域（2048 族） | p ≤ 5（p7 门 46/2048 失配，定理 3） | p ≤ 7（p9 双饱和） | **标准** |
| σ 硬化容量 | 2(23−q)=+14 bit（σ+15 时 SQ 过/标准失效，实测） | 0（Bg 可调但那是 q 旋钮本身） | **SQ** |
| 标量速度 | −4.5~−7.3% 内核（全自举 686/SQ=1.117×） | 基准 | **SQ** |
| 矩阵 r-lane 速度 | 平价 1.004–1.016（分解已被批处理共享） | 基准 | 平 |
| 每外积舍入 | 2^{−q−1}=2^{−17}（280 轮累计成 p≥7 绑定地板） | 2^{−24} | **标准** |
| 秘密类覆盖 | binary（多体 SQ 已实测） | binary（多体）＋ρ-SAB 标量 | 标准（暂时，G-ρ 后拉平） |
| 角色 | 修正安全下的工作核 | 高精度行回退 + 不可变 oracle | 互补 |

**结论与决策规则**：不可完全替代，也不应替代——正确设计是**核选择
规则**（= 设计文档 C6 混合核调度）：

```text
给定消息精度 p 与安全所需 σ 余量 m_sec：
  q* = max(p + 10, 23 − m_sec/2)          # 双约束的可行域下确界
  若 q* ≤ 23 可行 ⇒ 用 SQ(q*)             # p ≤ 5 时 q*=16 即 FINAL 主表
  否则（p ∈ {6,7} 等高精度行）⇒ 用标准核（或 SQ(q=23)≡标准）
```

即在"修正安全 ≥128 + p≤5"的主表工况，SQ **事实上已是工作核**（矩阵
基线 + SQ 组合全门通过）；6–7 bit 行与正确性 oracle 保留标准核。两核
共存不是妥协，而是同一参数族在精度—安全平面上的两个工作点。

## 6. 统一族陈述与"完备性"的精确边界（2026-09-04，回应用户理解）

**用户理解**："SQ 是通用概念，现有矩阵外积的分解手段是 SQ 的特例。"

**精确表述（论文措辞以此为准）**：

1. **在单数位机制域（l=1）内成立**：标准 ⊡ = SQ(q=Bg) 的破坏性、
   固定 q=23、未参数化的特例。SQ 的"通用性"= 把**操作数表示不变量**
   （scale-invariant external product）提升为外积设计的一等维度。
   该概念归属 ePrint 2025/1711（本团队前作）——"通用概念"的提法必须
   挂 1711 引用与作者重叠披露；本工作贡献 = 稀疏摊销 SAB 语境的
   instantiate/adapt/prove（定理 1–3）+ σ 硬化应用 + p 域闭环。
2. **限定**：外积的完全一般族是两参数 (l, Bg) 数位预算族。SQ 现有
   定理只覆盖 l=1 线；l>1 多数位积（如电路级 l=8/Bg=2^8）不是现 SQ
   的特例，而是族的多数位侧。"SQ 推广到 l 数位"（维持 l 个尺度分量
   不变量）原则上是自由的理论扩展，会收敛回标准多数位积 + 不变量
   维护收益——列为可选义务 T2b，未立项不主张。
3. **完备性校准**：核层（L3）叙事至此自洽闭合（两核 = 一族 + 固定
   汇率 + q* 选择规则）；但**完整矩阵自举算法（外层）仍有已立项缺口**
   （T1/T2 形式化、G-ρ 秘密类完备、C5 尾声、C1/C6/C7）——按
   `outer_layer_execution_plan.md` 推进，不提前收口。
