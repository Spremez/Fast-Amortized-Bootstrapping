# 最终参数基准总体规划（dell 迁移版，2026-09-02）

> 用户指令：E4 安全组合估计为当前唯一在途关键路径；其结论决定最终参数推荐；
> 本项目全部贡献 benchmark 与对比实验须在**最终参数**下重跑（安全公平性）；
> 后续所有实验与静机实测迁移至 **dell = 192.168.107.120**（用户 delld，
> 密钥 codex_delld_192_168_107_220），工作区 **~/spz/dell-final-bench**。
> 旧服务器（111.172）未闭合项可等待。

## 机器档案

| 项 | dell (107.120) | 旧服务器 (111.172) |
|---|---|---|
| CPU | Xeon Gold 6230R @2.10GHz, 104 核（**同型号**）| 同型号 |
| 内存 | 251 GB | — |
| AVX-512 / sage / gcc | ✓ / ✓ / 11.4 | ✓ / ✓ / 13.3 |
| 负载 | **0.2（静机）** | 15–143（共享污染源）|
| 磁盘 | ⚠ 仅 19G 空闲（只留源码+日志，及时清 tgz）| — |

同型号 CPU 使跨机数值可比性最好，但仍以 dell 内部比值为主指标、跨机标注为辅。

## Phase 0（已完成）
工作区就绪：源码树 @61e7c95 + hybrid_attacks_code（子模块已 init、__init__ 已修复）。

## Phase 1：E4 安全组合终裁（dell，运行中 e4v3）
- 输入密钥层：HD 网格直扫（绕开 dual_scale 预跑的域错误）h={39,41,42,52}
- 两层 uSVP/dual（老估计器顶层 API）
- 已有：BSK HD = 2^136.2@σ2⁻⁵⁰ / 2^175.1@σ2⁻³⁹（旧服务器，dell 复核）
- 1060：无公开代码 → 用本仓库 hybrid_dual（CHHS19 基线，1060 的改进对象）跑基线
  + 引用其论文实例表做参照；论文如实标注
- **产出**：组合 min 表 → 最终参数决策（候选：h=42+σ+0 / h=42+σ+11 / h=41+σ+11 / h=52 平衡档）

## Phase 2：最终参数下的全量公平基准（dell，静机协议：负载门控 <10，逐 case）
1. 构建矩阵：make + 探针（probe_sq / probe_pvw_sq / stock+pathA 双构建）
2. 标量 2×2：{SQ, 686} × 最终参数，9 轮中位 + 噪声 + gate
3. r-lane：{stock, SQ, 路径A} × r={2,4}，计时（3 trial×3 rep 背靠背）+ 噪声（10-trial make 路线）
4. **同场次锚点**：标量×2、×4 与 r-lane 同场 → 干净 "vs 686×r" 列
5. r-scaling：r={2,4,6,8} × {stock, pathA}（dell 静机出干净曲线，闭合旧服务器的负载疑点）
6. 容量曲线（postops 电路键 σ_G 扫描）@ 最终参数
7. 密钥大小 / RSS / keygen 时间（含 RS 重采样次数——E1 显示 h=42@t7 E[attempts]≈70）
8. **跨机一致性标定**：h=41σ+11 子集在 dell 复测一次 vs 旧服务器数字 → 论文跨机标注系数
9. A0 参照（686 原参数）同场重测 → 2×2 四格在 dell 全部重出

## Phase 3：收尾
- E3：packing/HWR 层组合审计（E4 工具复用）
- 失效概率三层证据（解析 + Clopper–Pearson + 稀有事件）
- 数据冻结 → 论文正文（安全列、参数表、全部主表按 Phase 2 新数重写）

## 排程与依赖
```
E4v3(dell, ~数小时) ─→ 参数终裁 ─→ Phase2 全量(~1-2 天静机) ─→ 数据冻结 ─→ 论文
                └→ E3 并行（工具就绪即跑）
旧服务器：全部 stage 已 COMPLETE，无未闭合项；不再排队新实验
```

## 纪律（延续）
- 静机门控 <10（dell 常态 0.2，几乎不会等待）
- 只用逐进程背靠背比值做主指标；绝对值带负载/跨机标注
- 每阶段日志回传本地 repro/ 并 git 提交
