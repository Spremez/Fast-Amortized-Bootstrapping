# 安全修正重构评估：对照一手资料逐条核实（2026-09-01）

> 输入：外部联合定参框架（h–σ–B–ℓ Pareto 方向）。本文档逐条对照一手资料
> （ePrint 2025/686 实现、2026/279、2026/366、2026/1060）核实其主张，
> 区分：**采纳**（真实缺口）/ **已满足**（有证据）/ **保留意见**（有理由），
> 并给出实验计划。禁猜测：每条注明证据来源。

## 一、一手资料核实结果

| 资料 | 核实内容 | 状态 |
|---|---|---|
| **2026/279** (Ogilvie) | 等距 hybrid：派生样本共享公开矩阵；稀疏秘密 RLWE **最多 15 bit** 间隙（λ 口径）；无代码 | ✅ 此前已核实 |
| **2026/366** (Hou & Jiang, CRYPTO'26) | 增强混合**解码**攻击：利用环结构加速 guessing/decoding；稀疏秘密设置下**线性于 N** 的改进；对 FHE 稀疏 Ring-LWE 参数集**最多降 13 bit、16 集中 12 个跌破 128**；方法论与 lattice-estimator 兼容；无代码 | ✅ **今日核实（摘要）** |
| **2026/1060** | May'21 MITM + 对偶格混合，改进 Bi-Lu-Luo-Wang (ACISP'22)；**稀疏三元**秘密 FHE 参数；对加速 BGV 最极端 **降 18 bit**；不依赖被 Ducas–Pulles 批判的独立性启发式；无代码 | ✅ **今日核实（摘要）** |
| **686 实现**（本仓库） | ①`gen_sparse_array`：支撑均匀随机、**无显式 gap**；三元符号**交替**（`val *= -1`）②`RS_sparse_*_key`：**rejection 采样至 max-gap ≤ 2^target_r_prec**（探针默认 7 → B≈2⁷=128）③分层 σ：σ_in=2⁻¹⁵ / σ_G(BSK)=2⁻⁵⁰ / packing=2⁻⁴⁴（h=256）/ HWR 由 t_ks=12 决定 | ✅ **今日读码核实** |

**实现侧新发现（此前所有安全分析未覆盖）**：
- **F1 条件分布**：输入密钥 = 均匀 h-子集 **条件于 max-circular-gap ≤ B**。T3 界用的
  log₂C(n,h) 是**无条件熵 → 高估安全**。B 越紧熵损失越大（我们的 B=128 远紧于外部框架的 N/4=512）。
- **F2 交替三元符号**：输出密钥（h_out=512）与 packing 的三元符号按生成次序交替，
  符号熵 ≈ 1 bit 而非独立符号的 h bit。1060 恰好攻击稀疏三元 → 这些层必须按真实分布重估。
- **F3 探针间不一致**：probe_sq 的 packing 是三元 h=256，probe_pvw_sq 是二值 h=256 —— 需按 686 规格统一。

## 二、逐条评估

### 采纳（真实缺口，已排实验）

| # | 外部框架主张 | 我们的缺口 | 行动 |
|---|---|---|---|
| A1 | 攻击组合须含 366/1060 | 我们的 σ+11 只按 279 校准；366 对稀疏环参数最多 −13 bit，可能吃掉 h=41 的余量 | **E4**：组合 {uSVP, dual/MATZOV, 279-H1/H2, 366, 1060} 的 λ(σ) 扫描（366 方法论 estimator-兼容，可按论文公式在服务器 sage 实现） |
| A2 | 条件分布/conditioning loss | F1：T3 熵高估 | **E1**：计算 gap-rejection 条件熵 T3′（B=128 vs N/4）|
| A3 | 三元符号独立性 | F2：交替符号 | **E1**：按真实符号分布重估输出/packing 层 + 核对 686 规格意图 |
| A4 | 审计全部密钥层 | 只估了 input+BSK；packing KSK/HWR 层未估 | **E3** |
| A5 | H_bits 生命周期余量 | 我们用朴素的 60−noise | 采纳 H_bits=½log₂(V_cap/V_out)（M=N_out·Q_BS、subgaussian z、b_det） |
| A6 | ρ=log₂(q/σ) 最大化方向 | 我们走的是 σ+11（降 ρ）分支 | **E2=stage363 已启动**：h=52 保原 σ（原 ρ）× B∈{2⁷,N/4} 实测，与 h=41+σ+11 对比噪声余量/速度 —— 两个分支都上 Pareto，不二选一 |
| A7 | 失败率三层证据 | 我们有解析界+0/50 MC | 补 rare-event 估计 + Clopper–Pearson 单侧区间（0/50 → p<0.06，不能声称 2⁻¹⁰⁰ 实证，只能作为解析界） |
| A8 | occupancy 25/50/100% | 未测 | 加入性能矩阵 |

### 已满足（证据在手，重构呈现即可）

| 外部框架要求 | 我们的证据 |
|---|---|
| 2×2 设计（A0/A1/B0/B1），头条 = A1 vs B1 | **四格全有**：A0=686@(39,σ+0) 13.3s；A1=686@(41,σ+11) 16.38s；B0=SQ/pathA@(39,σ+0)（stage358 σ+0 行）；B1=SQ/pathA@(41,σ+11)。头条本来就是同安全同机同场次 —— **审稿清单#9 我们已合规**，需按 2×2 重新表述 |
| 不同攻击不叠加、取 min | 我们的组合就是 min |
| 分层 σ 显式 | 实现本就分层（σ_in/σ_G/σ_PKS/σ_HWR），θ 向量化是写作任务（本文档 F 区已列实码值） |
| 逐阶段噪声实测+理论闭环 | stage359（r-lane 10-trial pair σ）、标量噪声 57.5–58.7、理论-实验闭环文档 |
| 新算法的精确操作计数 | Algorithm 1/2 + (h+1)ρn + dual-sub（pathA 算法文档） |
| σ 不改速度的实测 | 11.8 vs 11.6 s |

### 保留意见（有理由，非拒绝）

1. **其 h=52≈152 bit 与我们 h=41≈131.75 bit 不矛盾**：不同攻击层（279-H2 格侧 vs 我们
   T3 组合侧），组合 min 下各自在不同 h 处成为瓶颈。其平衡档预留 ~20 bit 是给未整合的
   366/1060 的**占位余量**；我们的 h=41+σ+11 是**最小修正点**。两者都是 Pareto 候选——
   E2 实测平衡档的代价（预期 ~+26% EP 计数），不预设谁优。
2. **SQ 部分解耦三难**：外部框架的 ρ-方向论证假设 686 型噪声耦合（σ_G 进主导项）。
   SQ 的 ∆ 抑制使 σ_G 项被压 32 bit——对 SQ 而言 h↑ 的代价近乎纯速度、噪声几乎不动。
   这是我们的结构性优势，框架未覆盖，论文应明确写出。
3. **工作量代理（1.47–1.84×）不采信**：直接实测（stage363）。

## 三、实验计划

| # | 内容 | 依赖 | 状态 |
|---|---|---|---|
| E1 | 条件熵 T3′（gap rejection 损失，B=2⁷ vs N/4）+ 交替符号层重估 + 686 规格核对 | 本地理论 | 待做（下一步） |
| E2 | 平衡档 h=52×B∈{2⁷,N/4}×σ+0：标量噪声/时间 + r-lane stock/pathA | 探针已加 SAB_SQ_RPREC 旋钮 | **stage363 运行中** |
| E3 | packing/HWR 层四攻击审计 | E4 工具 | 排队 |
| E4 | 组合估计器 λ(σ)（含 366/1060 公式复现） | 服务器 sage + 论文公式 | 排队（定稿必补） |
| E5 | 失败率三层证据 + occupancy 矩阵 | 探针扩展 | 排队 |

## 四、对方所需四项输入 → 我们的工件

1. 新算法伪代码+op 计数：`pathA_algorithm_and_differentiation.md`（Alg 1/2，(h+1)ρn，dual-sub）
2. 密钥分布：**本文档 F1/F2/F3**（二值条件 gap / 交替三元 / 分层 σ 实码值）
3. 各阶段实测方差：stage359（pair log₂σ=−6.6）、标量噪声表、stage-noise 闭环
4. 目标：3-bit 消息、batch 2048、r=4；**ε_life 与后续电路深度待用户给定**

## 五、stage363 预期读数（收割后填）

- h=52 σ+0 的标量噪声（预期显著低于 h=41 σ+11：BSK 项 2⁻⁵⁰ vs 2⁻³⁹ 差 11 bit）
- 平衡档速度代价（vs h=41：EP 计数 53/42 ≈ +26%，实测为准）
- B=2⁷ vs N/4 的差异（rejection/熵 vs 调度）
