# stage356-F：公平性协议与跨线整合（2026-08-28）

## 一、三条指令的落地

1. **基准环境 = 服务器唯一**：自本节起所有论文计时以 spz（Xeon Gold 6230R, spqlios_avx512, `-march=native`, 确定性种子）为唯一基准。本地 WSL 数据降级为开发参考（已知偏差：本地 −4.5% vs 服务器 −9.7%，方向一致、幅度不同）。
2. **跨会话成果整合**：见下表，理论线（独立会话, main 9e4536a）的产出已直接消费进本协议。
3. **公平参数重设（本协议核心）**：以 CRYPTO'26（原 ePrint 2026/279, Hou–Jiang–Ogilvie）攻击模型为基准重设 h 后再对比。

## 二、整合总表（各线成果 → 相互消费关系）

| 线 | 关键成果 | 被谁消费 |
|---|---|---|
| 理论线（sparse_key_lambda_bounds, main 9e4536a） | λ 三档界公式、686 六组密钥逐一复估（T3: B1 −0.77 贴线、B4 −6.15/B6 −4.68 违例；279 Table 8 估计器档 117.3–122.4）、h* 表、旋转天花板 = log₂n | 本协议（公平点选取）；论文 T2/T3 定理（P1-2 素材就此齐备） |
| SQ 线（本分支） | v3 内核服务器 −9.7%、σ+15 硬化闭环 +1.1%、bind 契约 | 公平基准的主体；论文 T1 |
| stage355 服务器矩阵（main adf8007） | r-lane 1.5152×–1.8271× | 论文性能上界（stage357 将与公平点叠加） |
| D4 线 | P0-b 最小复现（稠密 LUT 乘子 FFT 动态范围悬崖） | 修复在途（阈值截断候选） |
| P0-a（main 206903b） | AVX-512 9 轮协议、σ+15 服务器复现 | 本协议复用同一协议 |

## 三、公平点推导（消费理论线数字）

- SET_2_3_2048 输入密钥 = 理论线 B1（n=2048, h=39, σ=2^-15, δ=7.37）：纯组合 T3 天花板 128.13（贴 128 线，−0.77 vs 686 声明 128.90）；279 Table 8 估计器档（MitM-H2，含格约减成本）把 686 全族压到 117.3–122.4。
- **公平点 h=42**（= B2 重量，n 不变）：T3 天花板 +8.4 bit（(291.48−274.64)/2），按 MITM 档 λ≈(H−损失)/2 的标度恰好覆盖估计器档 6.7–10.5 bit 的亏空 → 两方案（SQ 与 stock scalar）同在"估计器档 ≥128"的保守公平点上实测。
- **锚点 h=39**：T3 组合档 128.13 的原点，1 轮连续性对照。
- 注：h↑ 使两方案外积数同增 ~8%（(h+1)·ρ·N 标度），绝对时间变慢、比值预期保持——这正是公平对比的意义：**同真实安全级下比速度**。σ 侧（输出密钥 2^-50）两方案相同，不动。

## 四、执行（已在途）

- `SAB_SQ_H` 环境参数（本提交）注入 probe；本地 h=42 冒烟 gate Pass。
- 服务器 runner `run_server_fair.sh`（已启动，PID 430412）：AVX-512 构建 + h=42 九轮背靠背比值 + h=39 锚点轮；后台任务 55 分钟后自动收割入 `fair_harvest.log`。
- 结果进 `fair_timing_summary.csv` 后，本节补"公平点上的 SQ vs scalar 比值"一行——这是论文评测节的最终形态数字。

## 五、对论文的影响

- 评测节全部表格改为"公平安全级（CRYPTO'26 档）+ 服务器实测"双列：h=39（T3 档）与 h=42（估计器档）。
- 理论节的 T2/T3 定理直接引用 sparse_key_lambda_bounds 的三档分层（[P]/[H]/[E]），+15 bit 保守插值的正当性由"天花板 log₂n=11 + ≥4 bit 裕量"论证收口。
- 686 原参数集在估计器档低于 128 的事实如实写入（这是本工作的动机之一：**公平修正后的重新基准**）。

## 六、结果（2026-08-28 收割，fair_timing_summary.csv）

| 点 | SQ/scalar 比值 | 判读 |
|---|---|---|
| **h=42（估计器档真 128-bit 公平点）** | **median 0.8978（快 10.2%）、min 0.8900、mean 0.9005** | 9/9 gate Pass，噪声 57.60/57.69 持平 |
| h=39 锚点（T3 组合档 128.13） | 0.8989（与 P0-a 的 0.9025 同域） | Pass，57.45/57.43 |

**结论**: 公平重参数化（CRYPTO'26 档、两方案同 h 同 σ）下 SQ 优势保持且略增（10.2% vs 9.7%）——h↑ 对两方案同征加压（外积数 +8%），SQ 的常数优势不受安全修正稀释。论文评测节的最终形态数字：**同真实安全级（估计器档 ≥128）下 SQ(q=16) 比 stock 快 10.2%（服务器 AVX-512 实测）**。中途进程一次被环境回收（round 5），setsid 续跑补齐，协议不变。
