# Stage305 Experiment Plan

Run selected-control and direct-DFT once each with `SAB_PVW_BODY_PROFILE=true` and `MAT_TRGSW_SPLIT_PROFILE=true`; parse component shares and choose the next micro-hypothesis.
