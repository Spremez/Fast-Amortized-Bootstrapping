# P-5 执行：NT-1 记号 lint + 措辞定稿 + 引用对齐记录

Date: 2026-09-09
义务：stage400 WS-C / P-5（0.5 会话）。

## 一、NT-1 记号 lint 结果（paper_full_v2 正文）

| 检查 | 结果 |
|---|---|
| 正文记号单一来源（stage400 §1 表） | ✓：n/N/d/r/ρ/h/U_a/Ψ/R₂/H1-H5 仅按表使用；代码名仅附录 D |
| "686" 口径 | ✓：§7.1 显式声明 = SAB 官方 CCS'25 工件；全部比值 = 686 时间/我方时间 |
| "矩阵基线构建" vs "686" | ✓：正文统一 686；矩阵基线（matrix-base）仅附录 D 语境 |
| σ 记号 | ✓：σ_G/σ_in/σ_KS/σ_EP 带 never-bare 下标；Lemma 1.7 的 σ₁σ₂ 在 §4 域内使用 |
| 草稿/内部文件引用 | ✓：正文 0 处 "draft/V2 草稿/stage"（D-3 规则）；单一来源指针仅在文首 provenance 头（打包时移除）|
| 单位/致谢 | ✓：匿名版无 |

## 二、Bergerat 措辞定稿（stage388 补审结论应用）

§8 已替换为 stage388 §2 的定稿段落：**"共享掩码批处理非本工作首创"
（Bergerat CM 稠密域 + 1711 代数形式）**；本工作定位 = 稀疏小钥匙域
的完整实例化（(h+1)ρn 适配、RS 间隙钥、Ψ/U_a/终倍增、DualSubCMUX
k=2 上界、修正安全定参、全量实测）。✅/❌ 措辞清单已入 stage388。

## 三、引用对齐（BibTeX 生成前的键清单）

MS18 / GPV23 / GP25(SAB=CCS'25) / BB26(BatchBoot USENIX'26) /
Careful26 / Ber25(TCHES'25) / Zam22(TFHE-rs) / ePrint 2025/1711 /
686=GP25 同件（正文 "686" 全部解析为 [GP25] 的实现）。
BibTeX 生成 = 投稿打包步（D-3 之后）。

## 四、两版冻结机制

打包脚本（P-4 清单）生成 anonymous/ 与 eprint/ 两目录；diff 仅允许
作者块/致谢差异，正文 diff 必须为空（CI 式检查随打包脚本执行）。

## 五、状态

- [x] NT-1 六项检查通过
- [x] Bergerat 措辞定稿应用
- [x] 引用键清单
- [x] 两版冻结机制定义
- 端到端打包执行（含 BibTeX/sed 脱敏实操）= 投稿打包步（下一会话或
  D-4 内审后）
