# Candidate D D2 Operator Closure

## Decision

`PASS_D2_OPERATOR_CLOSURE_G_LE_4`

## Construction

The scalar binary SAB schedule acts on a test-vector-carrying accumulator list. Setup rotates slot j by the public monomial X^{b_j}; each sparse step applies the full `r_prec` RGSW monomial butterfly (per bit i, non-wrapped slots take the CMUX source U_{j-2^i}, wrapped slots take the NCMUX source -tau_{-1}(U_{n-2^i+j}), realizing the negacyclic rotation X^{2^i} with X^N = -1), then applies `sub_a`, a per-slot public monomial rotation by X^{-a_j}.

Candidate D replaces the LUT-carrying accumulator by the LUT-independent operator state U = (U_identity, U_tau) per slot, representing the linear operator O = U_identity * id + U_tau * tau_{-1} in the algebra generated by monomial multiplications and tau_{-1}. The update laws are:

- public monomial rotation (setup, sub_a): both channels multiply by the monomial;
- CMUX selector mu: channel-wise affine selection U0 + mu*(U1 - U0);
- NCMUX wrapped source: -tau_{-1} acts by swapping the two channels and applying tau_{-1} to each, then negating (tau(U_tau), tau(U_id) -> negated);
- late binding L_F(U) = F * U_identity + tau_{-1}(F) * U_tau per slot, for any supported public integer LUT polynomial F.

## Closure equations

The operator algebra closes over the basis Gamma = {id, tau_{-1}} with structure constants e_id * e_id = e_id, e_id * e_tau = e_tau, e_tau * e_id = e_tau, e_tau * e_tau = e_id (tau_{-1}^2 = id), and the skew rule M * tau_{-1} = tau_{-1} * tau_{-1}(M) for monomials M. Hence |Gamma| = 2 <= 4.

The central invariant phi_0(L_F(Update(U))) = phi_0(ScalarUpdate(L_F(U))) holds for every update and every basis vector F = X^e because monomial multiplication commutes with the channel decomposition, CMUX is channel-wise affine, and L_F(tau_{-1}(U)) = tau_{-1}(L_F(U)) by the skew rule. Linearity in F lifts the basis-vector check to arbitrary supported LUT polynomials, and per-slot independence lifts it to per-slot LUTs.

## Basis search and the single permitted revision

Gamma_0 = {identity} (one channel) FAILED the phase-equivalence sweep; first divergence: {'basis_index': 0, 'slot': 4, 'case': 'binary_all_one', 'n': 8}. The single permitted revision to Gamma_1 = {identity, tau_{-1}} closed on every registered case and basis vector.

## Coverage

- rings: GF(257)[X]/(X^8+1) and GF(257)[X]/(X^16+1); phase rows: 256; trace rows: 147
- selectors mu in {0,1} via binary_all_zero/binary_all_one and the per-bit mixed cases; every rotation offset 2^i (i < r_prec) and every wrapped-slot boundary is exercised by the one-hot and alternating schedules; setup/sub_a exponents sweep all offsets in [-N, N) across slots
- schedule shape anchored to production sources: primary SET_2_3_2048 in main.c has in_N=2048, h_in=39, target_r_prec=7; the checker runs h=4 sparse steps (include-zero cases run 2h)

## Negative controls

| control | invariant | status | first divergence |
|---|---|---|---|
| remove_tau_minus_one_channel | operator_basis_closure | DETECTED | N=8 case=binary_all_one basis=0 |
| omit_tau_minus_one_swap | ncmux_channel_permutation | DETECTED | N=8 case=binary_all_one basis=1 |
| positive_negacyclic_wrap | negacyclic_wrap_sign | DETECTED | N=8 case=binary_all_one basis=0 |
| skip_sub_a_rotation | sub_a_phase_equivalence | DETECTED | N=8 case=binary_all_zero basis=0 |
| coeff_one_fast_with_zero_selector | include_zero_fast_path_guard | DETECTED | N=8 case=binary_all_zero basis=0 |
| wrong_butterfly_source_index | rgsw_butterfly_schedule | DETECTED | N=8 case=binary_all_one basis=0 |

## Source binding

- `src/sparse_amortized_bootstrap.c`: `8c0953caca8c87fe6fe5a12334d1bdfb320ddadf2dbf12379c221361a9b28d82`
- `src/sab_pvw.c`: `6aaabf61f010e0154b826855286137afc39de9b2520ee09e2ca43d5accf5e2ac`
- `include/sab.h`: `fb1de44cd4e5756d908e7b0a5d0dd206b568c06d37d7da002357df37d1b6fd9c`
- `include/sab_pvw.h`: `50a0b389b1bca1159f9e4551ca0fe616aaf8ee77018d3a27b6ce774b9e5b439e`
- `main.c`: `d402980a203aacbaf6b281a9f7245cf14b72c2c80468e5a05050f35373eb11f8`

## Scope limits

This checker establishes ideal algebraic semantics only. Torus scaling, gadget decomposition error, key sizes, noise growth, security hybrids, and the complete Amdahl projection belong to the D3 admission gate. No algorithm hot-path change is authorized by this document.

## Provenance

- input commit: `9adb9dd46fbad8dc6e9e8e9075b48f2aac2d9467`
- controller commit: `cfb8f6330e6cafecc60db48e9f341af578debe14`
- stage: D2 (exact operator closure)
